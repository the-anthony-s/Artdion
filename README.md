# todo
- come up with a name
- come up with slogan
- do I really need `follow` model? `like` model seems identical
- finish `:show` on `photos_controller` -> download button, for sale, user mini-profile update, action buttons update, details update
- finish `:edit` and `:new` on `photos_controller`
- finish `:show` on `users_controller`