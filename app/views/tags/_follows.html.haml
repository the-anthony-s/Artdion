- if user_signed_in?
  - if current_user.follows?(tag)
    = link_to tag_follow_path(tag), method: :delete, remote: true, class: 'btnAction' do
      = icon 'fas', 'user-check'
  - else
    = link_to tag_follow_path(tag), method: :post, remote: true, class: 'btnAction' do
      = icon 'fas', 'user-plus'
- else
  = link_to new_user_registration_path, class: 'btn btn--small' do
    = icon 'fas', 'user-plus'