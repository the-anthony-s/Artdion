.container
  %h1= @classification.name
  %p= @classification.description
  %p= @classification.translation.locale

%div(class="section")
  %div(class="container container--media")
    %div(class="grid grid--photos")
      -# = turbo_stream_from "photos"
      -# = turbo_frame_tag "photos" do
      = render partial: 'photos/photo', collection: @photos