- show_info = true if show_info == nil || show_info != false
- show_user = true if show_user == nil || show_user != false

%div.col
  %div(class="photoCard" itemListElement="itemListElement" itemscope itemtype="http://schema.org/ListItem" id="#{dom_id(photo)}")
    %meta{:content => "#{photo.name}", :itemprop => "name"}/
    %meta{:content => "#{photo_counter}", :itemprop => "position"}/
    %meta{:content => "#{photo_url(photo)}", :itemprop => "url"}/
    
    %div.photoCard-overlay
    
    
    %div.photoCard-top
      %div.photoCard-user
        = link_to photo.user, class: 'photoCard-user__avatar' do
          = avatar(photo.user, type: :small)
        %div.photoCard-user__body
          %span.photoCard-user__name= name(photo.user)
    
    = link_to photo, class: 'photoCard-media', title: "#{photo.name}" do
      = photo(photo, type = :card)

    %div.photoCard-bottom
      %div.photoCard-buttons
        %span(id="PhotoCard#{photo.id}")
          = render partial: 'photos/card-likes', locals: { photo: photo }, cached: true
        %span btn
      %div.photoCard-ad
        sponsored



    -# - if show_info
    -#   %div.photoCard-info
    -#     %div.photoCard-info__container
    -#       - if show_user
    -#         %div.photoCard-user
    -#           = link_to photo.user, class: 'photoCard-link photoCard-link--user', rel: 'contact', title: name(photo.user) do
    -#             %span.display-name= name(photo.user)